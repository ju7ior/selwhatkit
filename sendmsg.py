from whtfuncs import message_new_client_loop
from whtfuncs import msg_with_image_loop

"""
Client dictionary: client phone number and course purchased
eg. clients = {"phone": "phone_number", "course": "course_name"}
    processedClients = [()]
    imagePath in jpeg
    professor as string
"""
# msg_with_image_loop(clients, processedClients)
# message_new_client_loop(clients, processedClients, imagePath, professor)